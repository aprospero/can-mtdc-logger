This tool allows propagating several values via MQTT from the following device:

- SOREL MTDCv5

Other devices may work as well.

CAN addresses are taken from SOREL CAN BUS Interface (SCBI) documentation.

